{{nav-bar signOut="signOut"}}

{{!-- <div class="container"> --}}
    {{!-- <h1>Boostrap Button Builder</h1> --}}

  {{#each flashMessages.queue as |flash|}}
    {{flash-message flash=flash}}
  {{/each}}

  <div class="wrapper col-sm-12">
    {{outlet}}
  </div>
{{!-- </div> --}}
