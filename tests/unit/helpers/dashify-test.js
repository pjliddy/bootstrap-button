
import { dashify } from 'bootstrap-button/helpers/dashify';
import { module, test } from 'qunit';

module('Unit | Helper | dashify');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dashify([42]);
  assert.ok(result);
});

